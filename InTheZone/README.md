In The Zone
===========


To build with gradle, run:

```bash
./gradlew shadowJar
```

Run server with

```bash
./runServerFat.sh <port=80> <...>
```

Run game with

```bash
./runGameFat.sh <port=80> <...>
```
